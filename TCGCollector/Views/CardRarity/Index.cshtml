@model IEnumerable<TCGCollector.Models.CardRarity>

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CardRarityName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdateDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CardRarityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdateDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CardRarityID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CardRarityID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CardRarityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
