@model IEnumerable<TCGCollector.Models.CardCat>

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CardCatName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdateDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CardCatName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdateDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CardCatID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CardCatID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CardCatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
